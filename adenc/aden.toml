[package]
name = "adenc"
version = "0.1.0"

[sources]
entry = "adk/compiler/entry/Main.aden"

[build]
profile = "debug"
target = "windows-x64"
output_mode = "fat"
output_entry = "main"
runtime_debug_allocator = "off"
runtime_cycle_collector = "on"

[adklib]
enabled = true
