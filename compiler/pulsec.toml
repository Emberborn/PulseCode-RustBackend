[package]
name = "pulsec-compiler"
version = "0.1.0"

[sources]
entry = "compiler/entry/Main.pulse"

[build]
profile = "debug"
target = "windows-x64"
output_mode = "fat"
output_entry = "main"
runtime_debug_allocator = "off"
runtime_cycle_collector = "on"

[authorlib]
enabled = true
